<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show</title>
    <%- include('../partials/files.ejs')%>
</head>
<body>
    <main>
        <div>
            <article>
                <% if(card !== null){ %>
                    <% console.log(card) %>
                    <% if (card.name !== null){%><h2><%= card.name %></h2><%}%>
                    <h3><%= card.type_line %></h3>
                    <h3><%= card.expansion_symbol %></h3>
                    <h3><%= card.artist_info %></h3>
                    <% if (card.power_toughness != ""){ %>
                        <h3>Power/Toughness: <%= card.power_toughness %></h3>
                    <% } %>
                    <h3>Abilities: <%= card.text_box.abilities %></h3>
                    <h3>Flavor Text: <%= card.text_box.flavor_text %></h3>
                    <h3>Colorless Info: <%= card.mana_cost.colorless_info %></h3>
                    <h3>Color Identity: <%= card.mana_cost.colors_identity %></h3>
                    <div class="show_img"><img src="<%= card.img_link%>"/></div>
                    <form action="/card/<%=card._id%>?_method=Delete" method="POST">
                        <input type="submit" value="Delete This Card">
                    </form>
                <% }else{ %>
                    <% console.log(land) %>
                    <h2><%= land.name %></h2>
                    <h3><%= land.type_line %></h3>
                    <h3><%= land.artist_info %></h3>
                    <div class="show_img"><img src="<%= land.img_link%>"/></div>
                <%}%>
            </article>
        </div>
        <% if(card !== null){ %>
            <a href="/card/<%=card._id%>/edit"><button>Edit this card</button></a>
        <%}%>
        <a href="/card"><button>Back to Main Menu</button></a>
    </main>
</body>
</html>