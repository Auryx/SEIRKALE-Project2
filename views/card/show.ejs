<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show</title>
    <%- include('../partials/files.ejs')%>
</head>
<body>
    <main>

        <div>
            <article>
                <% if(card.toggle !== false){ %>
                    <div class="header-container"><h1><%= card.name%></h1></div>
                    <div class="show-container">
                        <div class="sub-card-1">
                            <% if (card.mana_cost.colorless_info != false){%><div class="show-text"><h3>Colorless Info: <%= card.mana_cost.colorless_info %></h3></div><%}%>
                            <% if (card.mana_cost.colors_identity != false){if (card.mana_cost.colors_identity != undefined){%><div class="show-text"><h3>Color Identity: <%= card.mana_cost.colors_identity %></h3></div><%}}%>
                            <% if (card.type_line != false){%><div class="show-text"><h3><%= card.type_line %></h3></div><%}%>
                            <% if (card.expansion_symbol != false){%><div class="show-text"><h3><%= card.expansion_symbol %></h3></div><%}%>
                            <% if (card.artist_info != false){%><div class="show-text"><h3>Artist: <%= card.artist_info %></h3></div><%}%>
                            <% if (card.collectors_number != false){%><div class="show-text"><h3>Collector's Number: <%= card.collectors_number %></h3></div><%}%>
                            <% if (card.power_toughness != false){%><div class="show-text"><h3>Power/Toughness: <%= card.power_toughness %></h3></div><%}%>
                        </div>
                        <div class="sub-card-2">
                        <% if (card.text_box.abilities != false){%><div class="show-text"><h3>Abilities: <%= card.text_box.abilities %></h3></div><%}%>
                        <% if (card.text_box.flavor_text != false){%><div class="show-text"><h3>Flavor Text: <%= card.text_box.flavor_text %></h3></div><%}%>
                        </div>  
                        <div class="show_img"><img src="<%= card.img_link%>"/></div>
                    </div>
                <% }else{ %>
                    <div class="header-container"><h1><%= card._doc.name%></h1></div>
                    <div class="show-container">
                        <div>
                            <div class="show-text"><h3><%= card._doc.type_line %></h3></div>
                            <div class="show-text"><h3><%= card._doc.artist_info %></h3></div>
                        </div>
                        <div class="show_img"><img src="<%= card._doc.img_link%>"/></div>
                    </div>
                <%}%>
            </article>
        </div>

        <div class="footer">
            <% if(card.toggle !== false){ %>
                <a href="/card/<%=card._id%>/edit"><button>Edit this card</button></a>
            <%}%>
            <a href="/card"><button>Back to Main Menu</button></a>
            <form action="/card/<%=card._id%>?_method=Delete" method="POST">
                <input type="submit" value="Delete This Card">
            </form>
        </div>
    </main>
</body>
</html>